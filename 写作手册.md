# 写作 & 维护手册

> 这个博客用 **Hugo + PaperMod 主题**搭建，托管在 **GitHub Pages**，push 后由 GitHub Actions 自动部署。
> 站点地址：https://hunterace832100.github.io/
> 你日常**只需要动 `content/` 目录和 `hugo.yaml`**，其它都是自动的。

---

## 一、新增一篇文章

在项目根目录（`personal_paper/`）执行：

```bash
# 1. 生成文章文件（文件名用英文短横线，会成为网址的一部分）
hugo new content posts/my-article.md
```

文件生成在 `content/posts/` 下，自带文件头（front matter）。用编辑器打开，改成：

```markdown
---
title: "文章标题"                 # 显示的标题
date: 2026-09-17T15:00:00+08:00
draft: false                      # 关键：false=发布，true=草稿(线上不显示)
math: true                        # 需要数学公式才加；不需要就删掉这行
tags: ["AI工程", "随想"]          # 标签，自动生成标签页
summary: "首页列表里显示的摘要"    # 可留空
ShowToc: true                     # 是否显示目录
---

正文用 Markdown 写……
```

分割线 `---` 下面就是正文。

## 二、本地预览（发布前强烈建议）

```bash
hugo server -D        # -D 表示连草稿一起预览
```

浏览器打开 http://localhost:1313/ ，改文件会自动刷新。看完按 `Ctrl+C` 停止。

## 三、发布上线

```bash
git add -A
git commit -m "post: 新增《文章标题》"
git push
```

push 后 GitHub Actions 自动构建部署，约 1 分钟后线上更新。
**不用自己生成 `public/`，全部交给云端。**

## 四、Markdown 常用写法速查

| 需求 | 写法 |
| --- | --- |
| 标题 | `## 二级标题`、`### 三级标题` |
| 加粗 / 斜体 | `**加粗**`、`*斜体*` |
| 行内代码 | `` `code` `` |
| 代码块 | ` ```python ` 开头、` ``` ` 结尾 |
| 链接 | `[文字](https://...)` |
| 图片 | 图片放 `static/images/`，写 `![说明](/images/xx.png)` |
| 引用 | `> 引用内容` |
| 行内公式 | `$e^{i\pi}+1=0$`（需文件头 `math: true`） |
| 独立公式 | 用 `$$ ... $$` 包起来（需 `math: true`） |

## 五、修改站点设置 / 页面

编辑 **`hugo.yaml`**：

- **站点标题**：顶部 `title` 字段
- **首页自我介绍**：`params.homeInfoParams` 的 `Title` / `Content`
- **顶部导航菜单**：`menu.main` 下增删项
- **社交图标（GitHub 等）**：`params.socialIcons`

新增独立页面（如「关于」）：新建 `content/about.md`，再在 `menu.main` 加一项指向 `/about/`。

## 六、注意事项

- 只改 `content/` 和 `hugo.yaml` 就够了；`public/`、`resources/` 是自动生成物，别手动改（已在 `.gitignore` 忽略）。
- 部署失败：去仓库 **Actions** 标签看红叉详情。
- 本机 Hugo 在 `~/.local/bin/hugo`（standard 版，v0.166.0）。

## 七、一句话循环

> 改文件 → `hugo server -D` 本地看 → `git add -A && git commit -m "..." && git push` → 等 1 分钟线上生效
